import { SignInForm } from "@/features/sign-in";

export const SignIn = () => {
  return (
    <main className="app-main">
      <section className="auth">
        <SignInForm />
      </section>
    </main>
  );
};
