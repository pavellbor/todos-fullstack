export type User = {
  username: string;
  token: string;
};

export type SignInUserData = {
  username: string;
  password: string;
};

export type SignUpUserData = {
  username: string;
  password: string;
};

export type VerifyTokenData = {
  username: string;
};
